apiVersion: v1
kind: Service
metadata:
  name: llm-frontend-canary-svc
spec:
  selector:
    app: llm-frontend
    version: canary
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
